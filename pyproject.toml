[project]
name = "redblock"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jason Qian", email = "jason.qian.dev@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
redblock = "redblock:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
    "jupyter>=1.1.1",
]
